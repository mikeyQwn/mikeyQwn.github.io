export class Instrument {
    constructor( ) {
        this.keyMap = keyMap;
    }
    getTabulature(notes) {
        for (const note of notes) {
            
        }
    }
}

class Note { 
    constructor(name, measure, beat)
}